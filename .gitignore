# Logs
*.log
log/
log_*.txt

# Game input data / large binaries (kept on disk, not tracked)
*.sqlite
*.xlsx
/backup/

# Generated HTML at repo root
/v*.html

# Backup / copy dirs
*- 副本/

# Generated output
/output/
/output_android/
/database/
/json/
/icons/
ChangelogResouce.json
*_log.txt

# Build
/target/
/build/

# Old git history backup
/.git.old.bak/

# Local maven classpath dump (machine-specific absolute paths)
/cp.txt

# Local machine paths (config.example.properties is the tracked template)
/config.properties

# Baidu Baike has no license we can redistribute under, so this one cached entry stays out
# of the repo. Everything else in manual/wiki/ comes from Wikipedia (CC BY-SA). To recreate
# it, follow the curl recipe in the comment above the CIVILIZATION_CHINA row of
# manual/wiki/_overrides.tsv; without it that page falls back to the game's own text.
/manual/wiki/zh_Hans_CN/CIVILIZATION_CHINA.json
